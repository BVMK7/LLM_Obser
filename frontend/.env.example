# Copy to .env and set to point the dashboard at a deployed backend instead
# of the local one. Leave unset (or delete .env) to use the local default
# (http://localhost:8010).
VITE_API_BASE=https://your-deployed-backend-url.example.com
