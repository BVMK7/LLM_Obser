.env
__pycache__/
*.pyc
.venv/
venv/

# Frontend
frontend/node_modules/
frontend/dist/

# Local dev server logs (created by the backend-restart workflow, not source)
uvicorn_out.log
uvicorn_err.log

# Large local-only reference asset, not project source
*.mp4

# Eval gate: transient output, regenerated every run — the baseline
# (eval/eval-baseline.json) IS committed, this summary is not.
eval_summary.md

# Generated by `pip install -e sdk/` — build metadata, not source.
sdk/llmobs.egg-info/
